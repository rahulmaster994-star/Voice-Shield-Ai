import SOCDashboard from "@/components/soc/soc-dashboard";

export const metadata = {
  title: "VOICE SHIELD AI — Live Call Protection Console",
  description: "Real-time voice impersonation detection & prevention system (SIH26104)",
};

export default function DemoPage() {
  return <SOCDashboard />;
}